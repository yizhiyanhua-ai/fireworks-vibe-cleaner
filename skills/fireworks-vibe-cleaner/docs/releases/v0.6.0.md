# v0.6.0 — Source-bound purpose evidence and useful next steps

Metadata alone did not explain why a particular session deserved a backup or retention. Triage now observes bounded local turn/plan records and accepts optional source-bound purpose annotations. Local-assistant judgments are explicit, not represented as user confirmation. Raw text, excerpts and summaries never go to Jev.

- Add `purpose-template`, `--purpose-notes`, `--skip-purpose` and explicit offline `--advisor rules`. Stale notes refuse before provider calls. Continuation needs retain originals; recorded pending plans block cleanup preparation without pretending the project is active.
- Keep original-preserving suggestions tentative below 0.5 confidence; low-confidence cleanup preparation still becomes review. Unknown-purpose new backup suggestions stay pending purpose review unless preserving history was explicitly requested. Raw model choices remain visible.
- Real 14-file comparison: rules with purpose took 113 ms; metadata Jev 1,447 ms; purpose Jev 1,155/1,213 ms including cached-purpose evidence collection. Initial local annotation effort and source-hash checks are excluded. Final rules and Jev reasons matched on 14/14 files, which does not establish accuracy or model superiority.
- Final results: 7 local keeps, 1 reuse of a verified copy, 1 existing-backup verification suggestion, 1 new-backup suggestion and 4 purpose reviews. Two new keeps come from local assistant judgments. Four raw speculative backup suggestions were held locally. No actual cleanup or disk recovery occurred; 9 checked source hashes matched.
- Preserve the initial probability-sum failure and earlier over-broad backup suggestions. Added bounded, disclosed two-decimal probability-sum compatibility without normalizing raw values; the exact original failure values were not captured.

See the bilingual README, [purpose contract](../purpose.md) and [complete sanitized real evidence](../experiments/real-purpose-comparison-2026-09-22.json). No user action labels, real Claude transcript coverage, visual understanding or native resume claims. All cleanup still requires exact-plan human approval and unchanged executor verification.
